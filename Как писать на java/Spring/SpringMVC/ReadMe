Сборка maven, сервер Tomcat, зависимости SpringCore, SpringMvc, thymeLeaf, конфигурация при помощи аннотаций (без .xml).
Hello word проект:
  -переход по ссылкам
  -между страницами, url с обязательными и нет параметрами
  -простой калькулятор
*GET-запросы, получение из него параметров и передача их через Model в View путём динамического отображения через thymeleaf
